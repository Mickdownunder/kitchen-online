# =============================================================================
# kitchen-online / BaLeah CRM – Umgebungsvariablen
# =============================================================================
# Kopiere diese Datei nach apps/crm/.env.local und ergänze die Werte.
# Niemals .env.local oder Secrets ins Repo committen.

# -----------------------------------------------------------------------------
# Supabase (Pflicht)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# -----------------------------------------------------------------------------
# App (Optional)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://crm.example.com
# Alternativ: NEXT_PUBLIC_BASE_URL

# -----------------------------------------------------------------------------
# E-Mail (Resend – für Buchungsbestätigung, Einladungen)
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_...
# Optional: E-Mail-Absender
EMAIL_FROM=office@example.com
BOOKING_EMAIL_FROM=office@example.com

# -----------------------------------------------------------------------------
# AI (Google Gemini – optional)
# -----------------------------------------------------------------------------
GEMINI_API_KEY=

# -----------------------------------------------------------------------------
# Webhooks & Cron
# -----------------------------------------------------------------------------
# Cal.com Webhook: Signatur-Verifizierung (optional)
CALCOM_WEBHOOK_SECRET=
# Vercel Cron: Schützt /api/cron/* (empfohlen in Produktion)
CRON_SECRET=

# Inbound-Dokumente (Lieferanten-E-Mails)
# Shared secret für /api/inbound/email/webhook
INBOUND_EMAIL_WEBHOOK_SECRET=
# Optional: Fallback-User-ID, falls Empfängeradresse keiner Company zugeordnet ist
INBOUND_DEFAULT_USER_ID=
# Optional: HMAC-Signatur für Resend Inbound Webhook
RESEND_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# E2E-Tests (Playwright)
# -----------------------------------------------------------------------------
PW_BASE_URL=http://localhost:3000
PW_CRM_EMAIL=
PW_CRM_PASSWORD=
PW_PORTAL_ACCESS_CODE=
# Oder: PW_PORTAL_EMAIL / PW_PORTAL_PASSWORD
